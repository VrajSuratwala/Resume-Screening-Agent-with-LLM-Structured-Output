````md id="jlwmo7"
# How to Run

## Step 1 — Clone Repository

```bash
git clone YOUR_GITHUB_REPO_LINK
cd YOUR_PROJECT_FOLDER
````

---

## Step 2 — Install Dependencies

```bash
pip install langchain
pip install langchain-core
pip install langchain-community
pip install langchain-groq
pip install pdfplumber
```

---

## Step 3 — Add Groq API Key

Inside the notebook or Python file:

```python
from langchain_groq import ChatGroq

llm = ChatGroq(
    groq_api_key="YOUR_GROQ_API_KEY",
    model_name="llama-3.3-70b-versatile"
)
```

---

## Step 4 — Run the Notebook

Open and run:

```text
Assignment_Final_with_Langchain.ipynb
```

Run all cells sequentially.

---

## Step 5 — Provide Inputs

The system will ask for:

1. Job Description Input
2. Resume PDF Upload

---

## Step 6 — AI Resume Analysis

The pipeline will:

* Extract resume text from PDF
* Analyze Job Description
* Compare Resume with JD
* Generate structured JSON output
* Calculate Match Score
* Generate Recommendation

---

## Step 7 — Final Output

Example Output:

```json
{
    "match_score": 78,
    "matched_skills": [
        "Python",
        "SQL",
        "REST APIs"
    ],
    "missing_skills": [
        "Docker",
        "AWS"
    ],
    "summary": "Candidate has strong backend development skills with relevant API and database experience.",
    "recommendation": "Potential Fit"
}
```

```
```
